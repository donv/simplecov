language: ruby

before_install:
  - gem install bundler

bundler_args: --without development --jobs=3 --retry=3

cache: bundler

sudo: false

rvm:
  - 1.8.7
  - 1.9.3
  - 2.0.0
  - 2.1
  - 2.2
  - ruby-head
  - jruby-1.7.24
  - rbx-2
  - jruby-9.0.5.0

matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-1.7.24
    - rvm: jruby-9.0.5.0
    - rvm: rbx-2
  fast_finish: true
